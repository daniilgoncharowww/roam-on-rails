<div class="max-w-xl mx-auto mt-10 bg-white p-6 rounded shadow text-black">
  <h1 class="text-2xl font-bold mb-6">Изменение категории: <%= @category.name %></h1>
  <%= render partial: "category_form", locals: { category: @category } %>
</div>
